[package]
name = "reactos-gui"
version = "0.1.0"
edition = "2021"
authors = ["ReactOS Rust Team"]
description = "Sistema GUI de ReactOS en Rust"

[lib]
name = "reactos_gui"
crate-type = ["lib"]

[dependencies]
# Dependencias básicas
libc = "0.2"
anyhow = "1.0"
log = "0.4"

[features]
default = []
